spring:
  application:
    name: test-actuator-app

management:
  endpoints:
    web:
      exposure:
        include: "*"
        exclude: mappings
      base-path: /actuator
  endpoint:
    env:
      show-values: always
    health:
      show-details: always
    heapdump:
      access: unrestricted
  info:
    env:
      enabled: true
    java:
      enabled: true
    os:
      enabled: true

info:
  build:
    artifact: test-actuator-app
    name: test-actuator-app
    version: 1.0.0
    group: com.example
  app:
    name: test-actuator-app
    description: Test Spring Boot application with Actuator

logging:
  file:
    name: /tmp/test-app.log
  level:
    root: INFO
    com.example.testapp: INFO
    org.springframework.web: INFO
